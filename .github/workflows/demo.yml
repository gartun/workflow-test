name: Demo
on: [push]
jobs: 
  prettier:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: prettify code
        uses: creyD/prettier_action@v4.0
        with:
          only_changed: True
       
