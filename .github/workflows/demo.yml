name: Demo
on: [push]
jobs: 
  checks-bats-version:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      with: 
        node-version: '14'
      - run: yarn add -g bats
      - run: bats -v
